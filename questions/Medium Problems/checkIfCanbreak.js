// const checkIfCanBreak = (s1, s2) => {
//   const s1Sorted = s1.split('').sort((a, b) => a.localeCompare(b));
//   const s2Sorted = s2.split('').sort((a, b) => a.localeCompare(b));
//   let boolCheck = true;
//   for (let index in s1Sorted) {
//     if (s1Sorted[index] >= s2Sorted[index]) {
//       boolCheck = false;
//     }
//   }
//   return boolCheck;
// };

// console.log(checkIfCanBreak('abe', 'acd'));
